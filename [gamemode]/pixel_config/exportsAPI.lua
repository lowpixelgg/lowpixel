function setDefault(key, value)
  return ConfigStorage.setDefault(key, value)
end

function set(key, value)
  return ConfigStorage.set(key, value)
end

function get(key)
  return ConfigStorage.get(key)
end
